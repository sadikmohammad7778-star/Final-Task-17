function Hero() {
  return (
    <section className="hero">
      <div>
        <h1>Build Modern Digital Experiences</h1>

        <p>
          We create responsive websites and scalable React applications.
        </p>

        <button>Get Started</button>
      </div>
    </section>
  );
}

export default Hero;